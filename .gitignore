output*
runtime-snapshot/snapshots/
*compare*.txt
cycles*.txt

# The driver scripts now live in refresh/ and are committed; MAIN_APP and ZW_APP
# override their defaults. local/ stays ignored for anything still machine-bound.
local/
